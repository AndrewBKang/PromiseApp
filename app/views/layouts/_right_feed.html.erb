<aside class="feed-right bubble glow">
	<ul>
		<h3>Friend Activity</h3>
		<% current_user.feed.each do |activity| %>
			<li>
				<figure><%= link_to(image_tag(activity[:picture]),user_url(activity[:user_id])) %></figure>
				<h4><%= link_to(activity[:username],user_url(activity[:user_id])) %></h4>
				<header class="cut-out">
					<div class="news">
						<%= "made a new #{activity[:type]} "%> 
						<%= link_to("'#{activity[:title]}'", resolution_url(activity[:activity_id]))%>
					</div>
				</header>
			</li>
		<% end %>
	</ul>
</aside>
