<li class="mini-window clearfix">
	<%= render :partial => 'shared/friend_profile', locals: {resolution: resolution} %>
	<%= render :partial => 'shared/resolution_header', locals: {resolution: resolution} %>
	<%= render :partial => 'shared/resolution_info_footer', locals: {resolution: resolution} %>
	<ul class='comments clearfix'>
	<%= render resolution.comments %>
	</ul>
</li>

<script>
$('.new_comment').on("ajax:success", function(event,data){
	$(this).parent().siblings('.comments').append(data)
});

</script>